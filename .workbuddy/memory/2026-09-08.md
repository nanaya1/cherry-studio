## 雪浪工匠模型服务默认地址更新

- 将 `packages/provider-registry/src/providers/xuelang.ts` 的默认 API 基址从旧内网 HTTP 地址更新为 `https://api.2dturing.com/v1`，官网地址更新为 `https://api.2dturing.com/`。
- 经只读端点验证，`/v1/models` 可达且要求 API Key，因此移除 `authOptional: true`，使客户端按必填鉴权处理。
- 保留现有模型 ID 与用户自定义地址覆盖，不增加迁移；无自定义覆盖的既有用户会读取新的注册表默认地址。
- 已重新生成 `packages/provider-registry/data/providers.json`，撤销生成脚本带来的无关模型目录刷新。
- 验证：provider-registry 测试 27/27 文件、404/404 用例通过；生成一致性检查通过；`git diff --check` 通过。

## 雪浪工匠模型服务域名最终调整

- 将雪浪工匠默认 API 基址最终调整为 `https://api.xuelanglm.com/v1`，官网地址调整为 `https://api.xuelanglm.com/`。
- 新域名根站与 `/v1/models` 均可达；模型接口要求 API Key，继续保持鉴权必填。
- 保留既有模型 ID 和用户自定义地址覆盖，不增加数据库迁移。
- 已同步生成 `packages/provider-registry/data/providers.json`，并撤销生成脚本造成的无关远程模型目录刷新。
- 验证：provider-registry 测试 27/27 文件、404/404 用例通过；生成检查与 `git diff --check` 通过。

## 雪浪工匠实时模型列表协调修复

- 确认雪浪工匠最终默认服务为 `https://api.xuelanglm.com/v1`，未将 2dturing 模型写入静态目录。
- 修正手动拉取模型的协调语义：上游 `/models` 成功时仅以实时结果作为权威集合；失败时回退静态目录且禁止 stale 清理；用户自定义模型不清理。
- 修改 `useProviderModelPullReconcile.ts` 及其回归测试，覆盖成功非空、成功空数组、失败降级和自定义模型保护。
- 验证：目标 Hook 测试 20/20 通过，新增权威源测试在临时还原旧逻辑后 2/2 失败；Web TypeScript、ESLint、Biome、`git diff --check` 均通过。测试进程退出阶段受当前 Node 22（项目要求 Node 24.11-24.15）影响出现 SIGTERM/137，但 Vitest 已完整报告 20/20 通过。
